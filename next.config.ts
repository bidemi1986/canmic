import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['plus.unsplash.com','images.unsplash.com','cdn.pixabay.com','www.canmic.com'],
  },
};

export default nextConfig;
